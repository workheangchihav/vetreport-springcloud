# ============================================================================
# Centralized Environment Configuration
# ============================================================================
# This is the single source of truth for all environment variables
# Used by: Docker Compose, Kubernetes, Backend, Frontend
# ============================================================================

# ----------------------------------------------------------------------------
# Cloudflare Tunnel Configuration
# ----------------------------------------------------------------------------
CLOUDFLARE_TUNNEL_NAME=demo-tunnel
CLOUDFLARE_DOMAIN=vetapi.mooniris.com,dev.mooniris.com
CLOUDFLARE_SERVICE=http://nginx:80

# ----------------------------------------------------------------------------
# Gateway & CORS Configuration
# ----------------------------------------------------------------------------
GATEWAY_CORS_ALLOWED_ORIGIN_PATTERNS=http://localhost:*,https://vetapi.mooniris.com,https://dev.mooniris.com,https://vetreport.vercel.app
ALLOWED_ORIGINS=http://localhost:3000,https://vetapi.mooniris.com,https://dev.mooniris.com,https://vetreport.vercel.app
COOKIE_SECURE=true

# ----------------------------------------------------------------------------
# Database Configuration
# ----------------------------------------------------------------------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# ----------------------------------------------------------------------------
# Monitoring Configuration
# ----------------------------------------------------------------------------
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# ----------------------------------------------------------------------------
# Backend Configuration (Spring Boot)
# ----------------------------------------------------------------------------
# JWT Configuration
JWT_SECRET=your-256-bit-secret-key-change-in-production-minimum-32-chars

# Docker Hub Configuration (for pulling pre-built images)
DOCKER_HUB_USERNAME=heangchihav
IMAGE_TAG=latest

# Database URLs (for local development)
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5433/user_service_db
